[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: pruebas con PostgreSQL/PostGIS (Testcontainers o DATABASE_URL en CI)
addopts = -v --strict-markers
filterwarnings =
    ignore::DeprecationWarning:passlib
