name: CI

on:
  push:
    branches: [main, master]
  pull_request:

env:
  PYTHON_VERSION: "3.11"
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_DB: exploro_test
  POSTGRES_HOST: localhost
  POSTGRES_PORT: "5432"
  DATABASE_URL: postgresql+psycopg2://postgres:postgres@localhost:5432/exploro_test
  SECRET_KEY: ci-secret-key-must-be-at-least-32-chars-long
  ALGORITHM: HS256
  ACCESS_TOKEN_EXPIRE_MINUTES: "60"

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgis/postgis:15-3.3
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: exploro_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
      - name: Pruebas unitarias (pytest + mocks)
        run: |
          pytest tests/unit \
            --cov=app \
            --cov-config=.coveragerc \
            --cov-report=term \
            --cov-report=
      - name: Integración PostGIS + cobertura combinada (>= 80 %)
        env:
          USE_TESTCONTAINERS: "0"
        run: |
          pytest tests/integration -m integration \
            --cov=app \
            --cov-append \
            --cov-config=.coveragerc \
            --cov-report=term-missing \
            --cov-report=xml \
            --cov-fail-under=80 \
            -v --tb=short

  load-test:
    needs: [test]
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgis/postgis:15-3.3
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: exploro_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
      - name: Esquema y API para Locust
        env:
          USE_TESTCONTAINERS: "0"
        run: |
          python - <<'PY'
          from sqlalchemy import text
          from app.database.connection import Base, engine

          with engine.connect() as c:
              c.execute(text("CREATE EXTENSION IF NOT EXISTS postgis"))
              c.commit()
          PY
          nohup uvicorn app.main:app --host 127.0.0.1 --port 8000 > /tmp/uvicorn.log 2>&1 &
          echo $! > /tmp/uvicorn.pid
          for i in $(seq 1 60); do
            if curl -sf http://127.0.0.1:8000/ >/dev/null; then
              echo "API lista"
              exit 0
            fi
            sleep 1
          done
          echo "Timeout esperando API"; cat /tmp/uvicorn.log; exit 1
      - name: Crear seed data para Locust
        run: |
          for user in \
            '{"nombre":"Turista 1","correo":"turista1@exploro.test","contraseña":"Test1234!","rol":1,"preferencias":[]}' \
            '{"nombre":"Turista 2","correo":"turista2@exploro.test","contraseña":"Test1234!","rol":1,"preferencias":[]}' \
            '{"nombre":"Pyme 1","correo":"pyme1@exploro.test","contraseña":"Test1234!","rol":2,"preferencias":[]}' \
            '{"nombre":"Pyme 2","correo":"pyme2@exploro.test","contraseña":"Test1234!","rol":2,"preferencias":[]}' \
            '{"nombre":"Admin","correo":"admin@exploro.test","contraseña":"Test1234!","rol":3,"preferencias":[]}'; do
            curl -sf -X POST http://127.0.0.1:8000/auth/register \
              -H "Content-Type: application/json" -d "$user" || true
            sleep 0.5
          done
          echo "Seed data creado"
      - name: Locust (50 usuarios, endpoints publicos, 45 s)
        run: |
          locust -f tests/performance/locustfile.py ListarLugaresPorCategoria \
            --host http://127.0.0.1:8000 \
            --headless -u 50 -r 10 -t 45s \
            --csv /tmp/exploro_locust
      - name: Verificar p95 < 200 ms
        run: |
          python scripts/check_locust_p95.py /tmp/exploro_locust_stats.csv --max-p95-ms 200
      - name: Detener API
        if: always()
        run: |
          if [ -f /tmp/uvicorn.pid ]; then kill $(cat /tmp/uvicorn.pid) || true; fi

  owasp-zap-baseline:
    needs: [test]
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgis/postgis:15-3.3
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: exploro_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
      - name: Esquema y API para ZAP
        env:
          USE_TESTCONTAINERS: "0"
        run: |
          python - <<'PY'
          from sqlalchemy import text
          from app.database.connection import Base, engine

          with engine.connect() as c:
              c.execute(text("CREATE EXTENSION IF NOT EXISTS postgis"))
              c.commit()
          Base.metadata.create_all(bind=engine)
          PY
          nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/uvicorn.log 2>&1 &
          echo $! > /tmp/uvicorn.pid
          for i in $(seq 1 60); do
            if curl -sf http://127.0.0.1:8000/ >/dev/null; then
              echo "API lista"
              exit 0
            fi
            sleep 1
          done
          echo "Timeout esperando API"; cat /tmp/uvicorn.log; exit 1
      - name: Resolver host.docker.internal (Linux / Actions)
        run: echo "127.0.0.1 host.docker.internal" | sudo tee -a /etc/hosts
      - name: OWASP ZAP Baseline (SQLi, auth, cabeceras)
        uses: zaproxy/action-baseline@v0.12.0
        continue-on-error: true
        with:
          target: "http://host.docker.internal:8000"
          cmd_options: "-a"
          allow_issue_writing: false
          fail_action: false
      - name: Detener API
        if: always()
        run: |
          if [ -f /tmp/uvicorn.pid ]; then kill $(cat /tmp/uvicorn.pid) || true; fi
